<!--
A real file, not a symlink to AGENTS.md. Frappe Cloud's marketplace audit blocks
an app that ships a symlink ("symlinks cause build errors"), and this one was the
single blocking finding. The line below is a Claude Code import, so the canonical
rules still live in AGENTS.md and are read from here.
-->

@AGENTS.md
