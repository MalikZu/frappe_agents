# Vendored code keeps upstream tau's formatting so cherry-picks stay readable.
# This file deliberately does not inherit the repo-root ruff settings.
line-length = 100
target-version = "py314"

[lint]
select = ["E", "F", "I", "UP", "B", "SIM"]

[format]
quote-style = "double"
indent-style = "space"
